# Build de production (chargé automatiquement par `vite build`).
#
# Ces deux valeurs sont PUBLIQUES par nature : la clé est une clé « publishable »
# (anon), la sécurité repose sur le RLS de la base. Elles finissent de toute façon
# dans le bundle navigateur. Les committer ici évite d'avoir à les saisir dans
# Vercel (où elles déclencheraient l'avertissement « préfixe public »).
#
# Les vrais secrets (service_role, PAT, RESEND_API_KEY) ne sont JAMAIS ici :
# ils vivent en secrets d'Edge Functions Supabase / .env.test.local local.

VITE_SUPABASE_URL=https://rpiypshtjvloopmzsdce.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_30-qutizs7nU75ihxxw_1A_Zm0S9MqP
