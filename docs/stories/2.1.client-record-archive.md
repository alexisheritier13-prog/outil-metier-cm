# Story 2.1: Fiche client complète et archivage

## Status

Done — CRUD client + archivage, coquille fiche à onglets, 4 tests d'intégration RLS + 3
tests composant verts.

## Story

**As a** Lead CM,
**I want** créer et modifier une fiche client complète et pouvoir l'archiver/réactiver,
**so that** le référentiel client reflète l'activité réelle de l'agence.

## Acceptance Criteria

1: Migration enrichissant `clients` : logo (URL), secteur d'activité, `is_archived`, timestamps, champ `archived_at`.
2: Formulaire de création/édition client (nom requis, logo par URL avec aperçu, secteur) accessible aux rôles `lead` et `admin` ; un `cm` a un accès lecture seule à ses clients.
3: Archiver un client le retire des vues de travail par défaut et des sélecteurs de nouveau post, mais conserve ses posts et son historique consultables via un filtre « archivés inclus ».
4: Réactiver un client le remet dans les vues par défaut.
5: RLS : lecture selon assignation/rôle (Story 1.3) ; écriture `lead`/`admin` uniquement ; tests d'intégration correspondants.

## Tasks / Subtasks

- [x] **Schéma** : `clients` porte déjà `logo_url`, `sector`, `is_archived`, `archived_at`, `deleted_at`, `deleted_by`, timestamps depuis la migration `0002` (front-load). Aucune migration supplémentaire pour cette story. (AC: 1)
- [x] `src/services/clients.ts` : `listClients(includeArchived)`, `getClient`, `createClient`, `updateClient`, `setClientArchived` (AC: 2, 3, 4)
- [x] `src/app/clients/useClients.ts` : hooks TanStack Query (AC: 2-4)
- [x] `src/app/clients/ClientForm.tsx` : rhf + zod (nom requis, URL logo validée, secteur) (AC: 2)
- [x] `src/app/clients/ClientsPage.tsx` (`/app/clients`) : liste, recherche par nom, toggle « inclure archivés », dialog création (lead/admin), état vide (AC: 2, 3)
- [x] `src/app/clients/ClientDetailPage.tsx` (`/app/clients/:clientId`) : en-tête (logo/nom/secteur/statut) + fil d'Ariane + **coquille à onglets** (Vue d'ensemble fonctionnelle ; Comptes sociaux / Contacts / Charte / Onboarding / Activité = placeholders) + boutons Modifier / Archiver-Réactiver (lead/admin) (AC: 2, 3, 4)
- [x] `src/components/ClientAvatar.tsx` (logo + repli initiales), `EmptyState`, `ui/tabs`
- [x] Route lazy `/app/clients` + `/app/clients/:clientId` ; entrée « Clients » dans `AppLayout`
- [x] `tests/integration/clients.integration.test.ts` (4) : lead crée/renomme/archive, CM ne crée pas, filtre `is_archived`, CM ne peut pas archiver (AC: 5)
- [x] `src/app/clients/__tests__/ClientsPage.test.tsx` (3) : liste + bouton gated par rôle + recherche + état vide

## Dev Notes

### Décisions

- **Pas de nouvelle migration** : `docs/stories/1.3` a créé `clients` avec toutes les
  colonnes de l'Epic 2 pour éviter une migration d'altération immédiate. AC 1 déjà couvert.
- **CM en lecture seule** : les boutons Modifier / Archiver ne s'affichent que pour
  `lead`/`admin` ; la RLS `clients_write` (0002) refuse de toute façon l'écriture aux
  autres. La charte (onglet Charte) fera exception : `cm` pourra l'éditer sur ses clients
  (Story 2.4).
- **Archivage ≠ suppression** : `is_archived` + `archived_at`. La corbeille (`deleted_at`,
  60 j) et son écran restent pour la Story 3.7. Le menu « Archiver ▾ » de la spec est réduit
  ici à deux boutons (Archiver / Réactiver).
- **Design monochrome** : appliqué (tokens noir & blanc, Inter). La couleur sémantique
  viendra plus tard (demande explicite « reste sur du noir et blanc pour le moment »).
- **Onglets placeholder** : Comptes sociaux, Contacts, Charte, Onboarding, Activité affichent
  un texte « arrive dans une prochaine story ». Ils seront remplis par 2.2 → 2.5 (+ Activité
  en Story 5.5).
- **Aperçu du logo** : `ClientAvatar` charge l'image avec repli sur les initiales si l'URL
  est absente ou cassée (`onError`). Pas d'upload — URL uniquement (conforme périmètre).

### Emplacements

- `src/services/clients.ts`, `src/app/clients/` (page liste, page détail, form, hooks, tests)
- `src/components/ClientAvatar.tsx`, `EmptyState.tsx`, `StatusBadge.tsx`, `ui/tabs.tsx`

### Standards

- Accès données via `src/services/` : respecté.
- a11y : `<label>` associés, `role="alert"` sur erreurs, fil d'Ariane `aria-label`, focus
  visibles (tokens `--ring`).
- Vue liste pleine largeur (`p-8`, pas de conteneur centré étroit).

### Testing

- **Intégration RLS** (`tests/integration/clients.integration.test.ts`, auto-skip) : via des
  clients Supabase authentifiés par rôle (helpers `service_role`).
- **Composant** : `ClientsPage` avec `@/services/clients` + `@/services/auth` mockés.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation — Epic 2 démarré | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run lint` / `typecheck` — OK
- `npm run test` — 61 tests verts (12 fichiers)
- `npm run build` — OK ; bundle initial ~474 kB (139 kB gzip) grâce au lazy-loading des routes

### Completion Notes List

- Routes passées en `React.lazy` + `Suspense` (bundle initial réduit, cf. front-end-spec §10).
- Onglets de la fiche client posés en entier ; seul « Vue d'ensemble » est fonctionnel.
- Aucune migration : schéma déjà en place depuis 0002.

### File List

- `src/services/clients.ts`
- `src/app/clients/ClientsPage.tsx`, `ClientDetailPage.tsx`, `ClientForm.tsx`, `useClients.ts`
- `src/app/clients/__tests__/ClientsPage.test.tsx`
- `src/components/ClientAvatar.tsx`
- `src/app/AppLayout.tsx`, `src/router.tsx` (modifiés)
- `tests/integration/clients.integration.test.ts`
- (design system : commit séparé `8dfe2ad`)

## QA Results

_En attente de `/qa`._
