# Story 2.4: Charte éditoriale par client

## Status

Done — migration `0005`, onglet « Charte », 2 tests d'intégration RLS + 1 composant verts.

## Story

**As a** CM,
**I want** consulter la charte éditoriale d'un client depuis sa fiche et depuis l'éditeur de post,
**so that** je rédige des posts conformes au ton et aux règles du client.

## Acceptance Criteria

1: Migration `editorial_guidelines` (client_id unique, ton de voix, mots à éviter, mots à privilégier, exemples de bons posts, guidelines visuelles — champs texte/markdown).
2: Section éditable sur la fiche client (rôles `cm` en lecture, `lead`/`admin` en écriture — ou `cm` en écriture sur ses clients, à trancher : par défaut `cm` écriture sur clients assignés).
3: La charte est consultable en un clic (panneau) depuis l'éditeur de post du client concerné.
4: RLS alignée ; tests d'intégration.

## Tasks / Subtasks

- [x] `supabase/migrations/0005_editorial_guidelines.sql` : table (`client_id` PK, 5 champs texte, `updated_at`/`updated_by`), trigger `set_updated_at`, RLS select+write = `has_client_access` (AC: 1, 2)
- [x] `src/services/editorialGuidelines.ts` : `getEditorialGuideline` (renvoie une charte vide si absente), `saveEditorialGuideline` (upsert sur `client_id`) (AC: 2)
- [x] `src/shared/types` : `EditorialGuideline` + `toEditorialGuideline` + `EMPTY_GUIDELINE`
- [x] `src/app/clients/tabs/GuidelinesTab.tsx` : formulaire 5 sections (ton, mots à éviter, mots à privilégier, exemples, guidelines visuelles) avec hints, une sauvegarde, état « Enregistré » transitoire (AC: 2)
- [x] Câblage onglet « Charte » dans `ClientDetailPage` (AC: 2)
- [x] `tests/integration/editorial-guidelines.integration.test.ts` (2) : CM assigné upsert + relit ; CM non assigné ni lecture ni écriture (AC: 4)
- [x] `src/app/clients/tabs/__tests__/GuidelinesTab.test.tsx` (1) : pré-remplissage + sauvegarde

## Dev Notes

### Décisions

- **AC 2 tranché : `cm` en écriture sur ses clients assignés** (`has_client_access` pour
  select ET write). La charte est un outil de travail du CM, pas une donnée d'identité du
  client (contrairement au nom/logo, réservés lead/admin en Story 2.1).
- **Une seule sauvegarde** plutôt qu'un enregistrement par section (la spec §4.2 évoquait le
  per-section). La table est une ligne unique ; le gain per-section est marginal en v1. À
  reconsidérer si la charte devient longue et souvent retouchée.
- **AC 3 (panneau depuis l'éditeur de post)** : reporté à l'Epic 3 (l'éditeur de post
  n'existe pas encore). Le service `getEditorialGuideline` est prêt à être réutilisé.
- **Pas de rendu Markdown en lecture pour l'instant** : champs `textarea` bruts. Un rendu
  `react-markdown` pourra être ajouté (front-end-spec §4.2) sans changement de modèle.
- **Charte vide par défaut** : `getEditorialGuideline` renvoie `EMPTY_GUIDELINE(clientId)`
  si aucune ligne — l'onglet est toujours éditable, la 1re sauvegarde fait l'`insert`.

### Emplacements

- `supabase/migrations/0005_editorial_guidelines.sql`
- `src/services/editorialGuidelines.ts`
- `src/app/clients/tabs/GuidelinesTab.tsx`
- `src/shared/types/index.ts`

### Testing

- **Intégration** : `tests/integration/editorial-guidelines.integration.test.ts` (auto-skip).
- **Composant** : `GuidelinesTab` avec le service mické.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run db:apply 0005` + `gen:types` — ok
- `npm run lint` / `typecheck` — OK
- `npm run test` — 79 tests verts (18 fichiers)
- `npm run build` — OK

### Completion Notes List

- `cm` peut éditer la charte de ses clients (décision AC 2).
- Consultation depuis l'éditeur de post = Epic 3.

### File List

- `supabase/migrations/0005_editorial_guidelines.sql`
- `src/services/editorialGuidelines.ts`
- `src/app/clients/tabs/GuidelinesTab.tsx`
- `src/app/clients/tabs/__tests__/GuidelinesTab.test.tsx`
- `src/shared/types/index.ts` (modifié), `src/shared/types/database.ts` (régénéré)
- `src/app/clients/ClientDetailPage.tsx` (modifié)
- `tests/integration/editorial-guidelines.integration.test.ts`

## QA Results

_En attente de `/qa`._
