# Story 2.5: Checklist d'onboarding client

## Status

Done — migration `0006`, onglet « Onboarding », auto-seed à la création client, 5 tests
d'intégration + 2 composant verts.

## Story

**As a** Lead CM,
**I want** une checklist d'onboarding par client avec suivi d'avancement,
**so that** aucun nouveau client ne démarre sans les étapes indispensables.

## Acceptance Criteria

1: Migration `onboarding_items` (client_id, libellé, `is_done`, ordre, `done_at`, `done_by`).
2: Un modèle d'étapes par défaut est créé automatiquement à la création d'un client (liste configurable par l'Admin dans les paramètres).
3: Sur la fiche client, la checklist permet de cocher/décocher, ajouter, réordonner et supprimer des étapes.
4: L'avancement (`x/y`) apparaît sur la fiche et dans la liste clients (Story 2.6).
5: RLS alignée ; tests d'intégration.

## Tasks / Subtasks

- [x] `supabase/migrations/0006_onboarding.sql` :
  - table `app_settings` (key/value jsonb) + seed `onboarding_template` (7 étapes par défaut) — RLS lecture=tous actifs, écriture=admin (AC: 2)
  - table `onboarding_items` (client_id, label, position, is_done, done_at, done_by) — RLS select+write = `has_client_access` (AC: 1, 5)
  - fonction + trigger `seed_onboarding_for_client()` : à l'`insert` d'un client, crée les items depuis `app_settings.onboarding_template` (AC: 2)
  - vue `client_onboarding_progress` (`security_invoker = on`) : `done` / `total` par client (AC: 4)
- [x] `src/services/onboarding.ts` : `list`, `setDone`, `add`, `remove`, `reorder` (AC: 3)
- [x] `src/app/clients/tabs/OnboardingTab.tsx` : checklist (case + libellé barré si fait), avancement `x/y`, ajout inline, suppression, **réordonnancement par flèches monter/descendre** (AC: 3)
- [x] Avancement `x/y` dans l'en-tête de la fiche client (`ClientDetailPage`) (AC: 4)
- [x] `tests/integration/onboarding.integration.test.ts` (5) : auto-seed, CM coche (`done_by`), add/remove, vue RLS, CM non assigné bloqué (AC: 5)
- [x] `src/app/clients/tabs/__tests__/OnboardingTab.test.tsx` (2)

## Dev Notes

### Décisions

- **`app_settings` créée ici** (key/value jsonb) : réutilisable pour les seuils d'alertes
  (Epic 8) et d'autres réglages. L'écran Admin d'édition du modèle d'onboarding viendra avec
  les Paramètres (pas bloquant pour cette story : le seed suffit).
- **Trigger `after insert` sur `clients`** lit le modèle et insère les items. `security
  definer` (le trigger doit écrire même si l'appelant n'a pas encore de policy pour ce
  nouveau client). Si le modèle est vide/absent → aucun item, pas d'erreur.
- **Réordonnancement par flèches** plutôt que drag & drop (`dnd-kit`) : la liste est courte
  (5–10 items), les flèches sont accessibles au clavier sans effort et évitent une
  dépendance. DnD reste une option de polish. `reorderOnboardingItems` réécrit toutes les
  positions selon l'ordre fourni.
- **Vue `client_onboarding_progress` en `security_invoker`** : sans ça, une vue Postgres
  s'exécute avec les droits de son propriétaire et **contournerait la RLS**. Testé.
- **`cm` peut cocher/gérer** les items de ses clients (`has_client_access`) — c'est du
  travail opérationnel.

### Emplacements

- `supabase/migrations/0006_onboarding.sql`
- `src/services/onboarding.ts`
- `src/app/clients/tabs/OnboardingTab.tsx`, `onboardingKeys.ts`

### Testing

- **Intégration** : `tests/integration/onboarding.integration.test.ts` (auto-skip).
- **Composant** : `OnboardingTab` avec le service mické.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run db:apply 0006` + `gen:types` — ok
- `npm run lint` / `typecheck` — OK
- `npm run test` — 86 tests verts

### Completion Notes List

- Vue exécutée en `security_invoker` pour ne pas casser l'isolation.
- Écran Admin d'édition du modèle : à faire dans les Paramètres (non bloquant).

### File List

- `supabase/migrations/0006_onboarding.sql`
- `src/services/onboarding.ts`
- `src/app/clients/tabs/OnboardingTab.tsx`, `onboardingKeys.ts`
- `src/app/clients/tabs/__tests__/OnboardingTab.test.tsx`
- `src/shared/types/index.ts` (modifié), `database.ts` (régénéré)
- `src/app/clients/ClientDetailPage.tsx` (modifié)
- `tests/integration/onboarding.integration.test.ts`

## QA Results

_En attente de `/qa`._
