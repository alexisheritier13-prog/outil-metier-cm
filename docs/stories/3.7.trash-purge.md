# Story 3.7: Corbeille et purge automatique

## Status

Done — migrations `0013`/`0014`, RPC de corbeille (règles FR45), restauration, `purge_trash()`,
page `/app/corbeille`. 5 tests d'intégration + 3 composant verts. **Epic 3 terminé.**

## Story

**As a** Lead CM,
**I want** que les suppressions de posts et clients soient réversibles pendant 60 jours,
**so that** une erreur ne détruit pas du travail définitivement.

## Acceptance Criteria

1: Migration ajoutant `deleted_at` / `deleted_by` à `clients` (posts déjà couverts en 3.1).
2: Page `/app/corbeille` listant posts et clients supprimés (rôles `lead`/`admin`), avec date de suppression et date de purge prévue (suppression + 60 j).
3: « Restaurer » remet l'entité exactement dans son état précédent (statut du post inclus) et la refait apparaître partout.
4: Fonction SQL `purge_trash()` supprimant définitivement les entités dont `deleted_at < now() - interval '60 days'` ; idempotente ; l'ordonnancement est branché en Epic 8.
5: Règles de droits FR45 appliquées et testées (CM ne supprime que ses brouillons ; purge manuelle avant 60 j = Admin seul).

## Tasks / Subtasks

- [x] `clients.deleted_at/deleted_by` **existaient déjà** (migration 0002) → pas de nouvelle colonne (AC: 1)
- [x] `supabase/migrations/0013_trash.sql` :
  - table `job_runs` (RLS lecture = admin) — journal des jobs, nécessaire à `purge_trash`
  - RPC `post_trash(id)` : CM → uniquement ses posts en statut `draft` ; Lead/Admin → tout post accessible ; pose `deleted_at/by`, journalise (AC: 5)
  - RPC `post_restore(id)` : Lead/Admin uniquement ; efface `deleted_at`, journalise (AC: 3)
  - RPC `client_trash(id)` / `client_restore(id)` : Lead/Admin (AC: 2, 3)
  - RPC `purge_trash()` : hard delete `deleted_at < now() - 60j` (posts + clients), écrit `job_runs`, idempotente (AC: 4)
  - RPC `trash_purge_now(entity, id)` : Admin uniquement, hard delete d'un élément (AC: 5)
  - garde `posts_guard_status_update` étendue : `UPDATE` direct de `deleted_at` (hors RPC) → `42501`
- [x] `supabase/migrations/0014_guard_allow_service_role.sql` : la garde ne vise que les rôles clients (`anon`/`authenticated`) ; `service_role` et `postgres` (RPC, jobs, back-office) sont de confiance
- [x] `src/services/posts.ts` : `trashPost` (via RPC + message FR sur refus CM), `restorePost`, `listTrashedPosts`, `purgePostNow`
- [x] `src/services/clients.ts` : `trashClient`, `restoreClient`, `listTrashedClients`, `purgeClientNow`
- [x] `src/app/trash/TrashPage.tsx` (`/app/corbeille`, `RequireRole` lead/admin) : sections Clients / Posts, date de purge prévue, boutons Restaurer, Purger (Admin) (AC: 2, 3)
- [x] `ClientDetailPage` : bouton « Corbeille » (Lead/Admin) → `client_trash` + retour à la liste
- [x] Route lazy + entrée nav « Corbeille » (Lead/Admin)
- [x] `tests/integration/trash.integration.test.ts` (5) : FR45 (CM brouillon oui / validé non, Lead oui), UPDATE direct refusé, restauration (Lead oui / CM non), purge immédiate (Admin seul), `purge_trash()` (> 60 j supprimé, récent conservé) (AC: 5)
- [x] `src/app/trash/__tests__/TrashPage.test.tsx` (3) : CM sans accès, Lead voit + restaure sans Purger, Admin a Purger
- [x] `tests/integration/posts.integration.test.ts` : test « soft delete » migré vers le RPC

## Dev Notes

### Décisions

- **`job_runs` créée ici** (référencée par l'archi pour l'Epic 8) car `purge_trash` doit s'y
  journaliser. Le cron (`pg_cron`) qui appellera `purge_trash` quotidiennement est branché
  en Story 8.4 ; ici la fonction est prête et testée, invocable manuellement par l'Admin
  (écran Paramètres → Jobs, Story 8.4).
- **Corbeille via RPC, pas UPDATE direct** : la garde `posts_guard_status_update` bloque
  désormais `UPDATE deleted_at` pour les rôles clients (défense en profondeur) — les règles
  FR45 ne sont pas contournables. `0014` a dû exempter `service_role` (utilisé par les
  tests et le back-office admin).
- **Restauration = Lead/Admin** : un CM ne restaure pas (il pourrait re-sortir de corbeille
  un post qu'un Lead a volontairement supprimé). La restauration efface juste `deleted_at`
  — le `status` n'a jamais été touché par la mise en corbeille, donc l'état est bien
  identique (AC 3).
- **Visibilité RLS** : depuis `0010`, les policies SELECT ne filtrent plus `deleted_at` →
  les éléments en corbeille restent lisibles via RLS ; `TrashPage` les requête avec
  `.not('deleted_at', 'is', null)`, et la page est gardée `lead`/`admin`.
- **Purge immédiate = Admin** : `trash_purge_now` réservé au rôle `admin` (FR45).

### Emplacements

- `supabase/migrations/0013_trash.sql`, `0014_guard_allow_service_role.sql`
- `src/services/posts.ts`, `src/services/clients.ts` (modifiés)
- `src/app/trash/TrashPage.tsx`

### Testing

- **Intégration** : `trash.integration.test.ts` (règles + purge).
- **Composant** : `TrashPage` (accès par rôle, boutons).

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation — Epic 3 terminé | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run db:apply 0013..0014` + `gen:types` — ok
- `npm run lint` / `typecheck` — OK
- `npm run test` — 60 unitaires verts (16 fichiers)
- `npm run test:rls` — 62 intégration verts
- `npm run build` — OK

### Completion Notes List

- Découverte pendant les tests : la garde `deleted_at` bloquait aussi `service_role` → `0014`.
- `job_runs` créée en avance (utilisée par `purge_trash`).
- Le cron de purge quotidienne = Story 8.4.

### File List

- `supabase/migrations/0013_trash.sql`, `0014_guard_allow_service_role.sql`
- `src/services/posts.ts`, `src/services/clients.ts` (modifiés)
- `src/app/trash/TrashPage.tsx`, `src/app/trash/__tests__/TrashPage.test.tsx`
- `src/app/clients/ClientDetailPage.tsx` (bouton Corbeille), `src/router.tsx`, `src/app/AppLayout.tsx` (modifiés)
- `src/shared/types/database.ts` (régénéré)
- `tests/integration/trash.integration.test.ts`, `posts.integration.test.ts` (modifié)

## QA Results

_En attente de `/qa`._
