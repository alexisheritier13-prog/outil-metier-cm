# Story 2.3: Contacts de validation multiples

## Status

Done — migration `0004` (`client_contacts` + `contact_client_ids()`), onglet « Contacts »,
Edge Function `admin-users` étendue (`invite_contact`), 4 tests d'intégration + 3 composant
verts.

## Story

**As a** Lead CM,
**I want** gérer plusieurs contacts de validation par client,
**so that** le bon interlocuteur client peut approuver les posts.

## Acceptance Criteria

1: Migration `client_contacts` (client_id, nom, email, `auth_user_id` nullable, `is_active`, timestamps).
2: Sur la fiche client, section CRUD des contacts, indépendante des autres champs.
3: Un contact peut être « invité » : création d'un compte `auth.users` + `profiles.role = client` lié via `auth_user_id`, ou rattachement à un compte existant.
4: Désactiver un contact lui retire l'accès à l'espace client sans supprimer son historique de validations.
5: RLS : gestion réservée `lead`/`admin` ; un contact client ne voit que sa propre fiche contact (le cas échéant) ; tests d'intégration d'isolation.

## Tasks / Subtasks

- [x] `supabase/migrations/0004_client_contacts.sql` : table `client_contacts` (`unique (client_id, email)`, FK `auth_user_id` → `auth.users` `on delete set null`), fonction `contact_client_ids()` (`security definer`, base des policies de l'espace client) (AC: 1)
- [x] RLS `client_contacts` : select = `has_client_access(client_id)` **ou** `auth_user_id = auth.uid()` ; write = `lead`/`admin` (AC: 5)
- [x] Edge Function `admin-users` — action `invite_contact` : appelant `lead`/`admin`, crée ou retrouve le compte `auth.users` de l'email, force `profiles.role = 'client'` + actif, upsert `client_contacts` avec `auth_user_id`, renvoie `actionLink` + `isNewAccount` (AC: 3)
- [x] `src/services/clientContacts.ts` : `list`, `add`, `update`, `setActive`, `remove`, `inviteClientContact` (invoke fonction) (AC: 2, 3, 4)
- [x] `src/app/clients/tabs/useClientContacts.ts` : hooks (AC: 2-4)
- [x] `src/app/clients/tabs/ContactsTab.tsx` : tableau (Nom · Email · Accès · Statut), ajout inline avec case « créer un accès », bouton « Inviter » par ligne, activer/désactiver, supprimer ; affiche le lien de mot de passe après invitation ; lecture seule pour `cm` (AC: 2, 3, 4)
- [x] Câblage dans `ClientDetailPage` (onglet « Contacts ») (AC: 2)
- [x] `src/shared/types` : `ClientContact` + `toClientContact` ; `database.ts` régénéré
- [x] `tests/integration/client-contacts.integration.test.ts` (4) : lead ajoute / CM non, CM assigné lecture seule, `invite_contact` crée le compte client + isolation (le contact ne voit que son client), CM ne peut pas appeler `invite_contact` (AC: 5)
- [x] `src/app/clients/tabs/__tests__/ContactsTab.test.tsx` (3) : CM lecture seule, Lead invite + lien, état vide

## Dev Notes

### Décisions

- **`contact_client_ids()` posée ici** (référencée par l'archi, absente de 0002/0003). Elle
  sera le pivot de toutes les policies de l'espace client (Epic 6) : renvoie les `client_id`
  du contact connecté, actifs uniquement.
- **Invitation via l'Edge Function existante** (`admin-users` + action `invite_contact`)
  plutôt qu'une nouvelle fonction : même mécanisme d'auth (JWT de l'appelant), même
  déploiement. L'appelant doit être `lead` ou `admin` (vs `admin` seul pour `create`).
- **Rattachement à un compte existant** : si l'email a déjà un `auth.users`, on le lie
  (`auth_user_id`) au lieu d'échouer — un même email peut être contact de plusieurs clients
  et réutilise le même compte de connexion.
- **`upsert` sur `(client_id, email)`** : ré-inviter un contact existant est idempotent
  (réactive + régénère un lien).
- **Désactivation** : `is_active = false` sur `client_contacts`. `contact_client_ids()`
  filtre dessus → l'accès portail est coupé immédiatement (RLS), sans toucher aux
  validations passées (elles vivront dans `post_history`, Epic 4/5).
- **CM en lecture seule** : la RLS `client_contacts_write` refuse aux `cm` ; l'UI masque les
  actions. Le `cm` voit les contacts (utile pour savoir qui valide) via
  `has_client_access`.

### Emplacements

- `supabase/migrations/0004_client_contacts.sql`
- `supabase/functions/admin-users/index.ts` (action `invite_contact`)
- `src/services/clientContacts.ts`
- `src/app/clients/tabs/ContactsTab.tsx`, `useClientContacts.ts`
- `src/shared/types/index.ts`

### Testing

- **Intégration** : `tests/integration/client-contacts.integration.test.ts` — inclut un vrai
  parcours d'invitation + connexion du contact + vérification d'isolation.
- **Composant** : `ContactsTab` avec services + auth mockés.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run db:apply 0004` + `npm run gen:types` — ok
- `supabase functions deploy admin-users` — ok (redéployée avec `invite_contact`)
- `npm run lint` / `typecheck` — OK
- `npm run test` — 76 tests verts (16 fichiers)
- `npm run build` — OK

### Completion Notes List

- Edge Function `admin-users` gère maintenant 2 actions ; le contrôle de rôle est par action.
- L'invitation régénère toujours un `actionLink` (type `recovery`) — pas d'email en v1.
- Historique des validations : les entrées survivront à la désactivation d'un contact (tables `post_history` à venir).

### File List

- `supabase/migrations/0004_client_contacts.sql`
- `supabase/functions/admin-users/index.ts` (modifié)
- `src/services/clientContacts.ts`
- `src/app/clients/tabs/ContactsTab.tsx`, `useClientContacts.ts`
- `src/app/clients/tabs/__tests__/ContactsTab.test.tsx`
- `src/shared/types/index.ts` (modifié), `src/shared/types/database.ts` (régénéré)
- `src/app/clients/ClientDetailPage.tsx` (modifié)
- `tests/integration/client-contacts.integration.test.ts`

## QA Results

_En attente de `/qa`._
