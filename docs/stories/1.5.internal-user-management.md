# Story 1.5: Gestion des utilisateurs internes

## Status

Done — Edge Function `admin-users` déployée, écran `/app/parametres/utilisateurs`, 6 tests
d'intégration (dont l'Edge Function) + 1 test composant verts. Epic 1 terminé.

## Story

**As a** Admin agence,
**I want** créer, modifier, activer/désactiver des utilisateurs internes et gérer leurs clients assignés,
**so that** je contrôle qui utilise l'outil et sur quel périmètre.

## Acceptance Criteria

1: Page `/app/parametres/utilisateurs` visible uniquement par l'Admin, listant les utilisateurs internes (nom, email, rôle, statut, nb de clients assignés).
2: L'Admin peut inviter/créer un utilisateur (nom, email, rôle) ; le compte est créé désactivé jusqu'à première connexion + activation, ou activé directement selon le choix.
3: L'Admin peut changer le rôle, activer/désactiver, et éditer la liste des clients assignés (multi-sélection) d'un utilisateur.
4: La désactivation empêche la connexion (cf. 1.4) et retire l'utilisateur des sélecteurs d'assignation de post, sans effacer son historique.
5: Toutes ces opérations sont refusées côté base (RLS/policies) pour les rôles `cm`, `lead`, `client`.
6: Tests d'intégration couvrant : création, changement de rôle, désactivation, modification d'assignations, et rejet pour non-Admin.

## Tasks / Subtasks

- [x] Edge Function `supabase/functions/admin-users/` : action `create` (vérifie l'appelant `admin` actif via son JWT, crée `auth.users` avec `service_role`, applique nom/rôle/activation sur le profil, rollback si échec, génère un lien de mot de passe) + `_shared/cors.ts` (AC: 2, 5)
- [x] Déploiement : `supabase functions deploy admin-users` (sans Docker, via `SUPABASE_ACCESS_TOKEN`)
- [x] `src/services/users.ts` : `listInternalUsers`, `listAssignableClients`, `getUserClientIds`, `createInternalUser` (invoke fonction), `updateUserRole`, `setUserActive`, `setUserClientIds` (AC: 1-3)
- [x] `src/app/settings/useUsersAdmin.ts` : hooks TanStack Query (list, create, updateRole, setActive, setUserClients) (AC: 1-3)
- [x] `src/app/settings/UsersPage.tsx` : tableau (nom/email/rôle/statut/clients), select de rôle inline, bouton activer/désactiver, bouton assignations ; ligne « soi » verrouillée (AC: 1, 3, 4)
- [x] `src/app/settings/CreateUserDialog.tsx` : formulaire rhf+zod (nom, email, rôle, activer) ; affiche le lien de mot de passe après création (AC: 2)
- [x] `src/app/settings/AssignClientsDialog.tsx` : liste de cases à cocher des clients, enregistrement via `setUserClientIds` (AC: 3)
- [x] `src/components/ui/dialog.tsx` (Radix Dialog) (AC: 2, 3)
- [x] `src/app/AppLayout.tsx` : coquille interne (nav Calendrier / Utilisateurs si admin, identité, déconnexion) + `<Outlet/>`
- [x] `src/router.tsx` : `/app` en route parente (RequireRole internes) avec enfants `index` (AppHome) et `parametres/utilisateurs` (RequireRole admin) (AC: 1)
- [x] Tests d'intégration `tests/integration/admin-users.integration.test.ts` (5) (AC: 6)
- [x] Test composant `src/app/settings/__tests__/UsersPage.test.tsx` (1)

## Dev Notes

### Décisions

- **Création de compte = Edge Function obligatoire.** Le navigateur ne peut pas détenir la
  clé `service_role` ni écrire dans `auth.users`. `admin-users` fait le contrôle d'accès
  lui-même (lecture du profil de l'appelant via son JWT) puis agit en `service_role`. Les
  variables `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` sont
  injectées automatiquement par la plateforme — aucun secret à configurer.
- **Pas d'email en v1.** La fonction renvoie un `action_link` (type `recovery`) que l'admin
  transmet manuellement au nouvel utilisateur pour qu'il définisse son mot de passe.
- **Changement de rôle / activation = `update` direct sur `profiles`** (pas d'Edge
  Function) : la policy `profiles_admin_all` (0002) l'autorise pour l'admin et le refuse
  aux autres. Idem `user_clients` via `user_clients_admin_write`.
- **Auto-protection** : dans l'UI, l'admin ne peut ni changer son propre rôle ni se
  désactiver (ligne verrouillée). Garde-fou UI ; une garde SQL pourra être ajoutée si
  besoin.
- **Assignations pour les admins** : bouton désactivé — un admin voit déjà tous les clients
  (`has_client_access`), les assignations n'ont de sens que pour les `cm`/`lead`.
- **AC 4 (retrait des sélecteurs de post)** : la partie « sélecteur de rédacteur » n'existe
  pas encore (Epic 3) ; la contrainte sera respectée à ce moment-là en filtrant sur
  `is_active`. La désactivation (connexion impossible + RLS) est effective dès maintenant.

### Emplacements

- `supabase/functions/admin-users/`, `supabase/functions/_shared/cors.ts`
- `src/services/users.ts`
- `src/app/settings/` (page + dialogs + hooks + tests)
- `src/app/AppLayout.tsx`

### Standards

- Accès données via `src/services/` : respecté.
- Edge Function : kebab-case, `_shared` pour le commun, imports `jsr:`/URL Deno.
- a11y : `aria-label` sur les selects de rôle par ligne, messages `role="alert"`, Dialog
  Radix (focus trap, Esc).

### Testing

- **Intégration** (`tests/integration/admin-users.integration.test.ts`, auto-skip sans
  `.env.test.local` / schéma) : admin crée un user via la fonction (rôle appliqué), CM
  refusé (403), admin change rôle + désactive (update direct), CM ne modifie pas un autre
  profil, admin gère `user_clients` mais pas le CM.
- **Composant** : `UsersPage` liste les users et verrouille la ligne de l'utilisateur
  courant.
- Nettoyage des orphelins `@example.test` : voir `tests/integration/README.md`.

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation complète — Epic 1 terminé | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run lint` / `typecheck` — OK
- `npm run test` — 54 tests verts (10 fichiers)
- `npm run build` — OK
- `supabase functions deploy admin-users` — OK sans Docker

### Completion Notes List

- Première Edge Function du projet ; déploiement possible sans Docker via le jeton d'accès.
- `admin-users` non protégée par une garde `verify_jwt` custom au niveau plateforme (défaut Supabase = JWT requis) + contrôle `role='admin'` dans le code.
- Route `/app` transformée en layout + routes enfants pour héberger `parametres/utilisateurs`.

### File List

- `supabase/functions/_shared/cors.ts`
- `supabase/functions/admin-users/index.ts`
- `src/services/users.ts`
- `src/app/settings/UsersPage.tsx`, `CreateUserDialog.tsx`, `AssignClientsDialog.tsx`, `useUsersAdmin.ts`
- `src/app/settings/__tests__/UsersPage.test.tsx`
- `src/app/AppLayout.tsx`, `src/app/AppHome.tsx` (simplifié)
- `src/components/ui/dialog.tsx`
- `src/router.tsx` (modifié)
- `tests/integration/admin-users.integration.test.ts`
- `package.json` (@radix-ui/react-dialog, @radix-ui/react-label)

## QA Results

_En attente de `/qa`._
