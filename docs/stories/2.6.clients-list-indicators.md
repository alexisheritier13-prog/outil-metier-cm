# Story 2.6: Liste des clients avec indicateurs

## Status

Done — migration `0007` (vue `client_overview`), `ClientsPage` enrichie (colonnes + tri),
1 test d'intégration + 3 composant verts. **Epic 2 terminé.**

## Story

**As a** Lead CM,
**I want** une liste des clients actifs avec des indicateurs de charge et d'activité,
**so that** je repère d'un coup d'œil les clients à risque.

## Acceptance Criteria

1: Page `/app/clients` listant les clients non archivés (nom + logo, secteur), avec bascule « inclure archivés ».
2: Colonnes/indicateurs : nombre de posts en attente de validation interne, en attente de validation client, avancement onboarding (`x/y`), date de dernière activité (dernier post créé ou modifié).
3: La liste est triable par chaque indicateur et cherchable par nom.
4: Un clic ouvre la fiche client ; la vue respecte les mémoires projet (pas de conteneur étroit, exploiter la largeur d'écran).
5: Les indicateurs sont calculés via une vue SQL ou une fonction, testée pour l'exactitude.

## Tasks / Subtasks

- [x] `supabase/migrations/0007_client_overview.sql` : vue `client_overview` (`security_invoker = on`) — client + `onboarding_done/total` (LEFT JOIN `client_onboarding_progress`) + `pending_internal`/`pending_client`/`last_activity_at` en **placeholders** (0 / `updated_at`) jusqu'à l'existence de `posts` (Epic 3) (AC: 2, 5)
- [x] `src/services/clients.ts` : `listClientOverview(includeArchived)` (AC: 1, 2)
- [x] `src/shared/types` : `ClientOverview` + `toClientOverview` (colonnes de vue nullable → défauts)
- [x] `ClientsPage` : colonnes Client / Secteur / À valider / Onboarding / Dernière activité ; tri cliquable sur Nom, Onboarding, Dernière activité (`aria-sort` sur `<th>`) ; recherche par nom ; toggle archivés (AC: 1, 3, 4)
- [x] `tests/integration/client-overview.integration.test.ts` (1) : le CM ne voit que ses clients dans la vue, avec le bon `onboarding_done/total` (AC: 5)
- [x] `src/app/clients/__tests__/ClientsPage.test.tsx` (3) — réécrit pour `listClientOverview` : liste + `x/y`, tri par onboarding, état vide (AC: 3)

## Dev Notes

### Décisions

- **`pending_internal` / `pending_client` / `last_activity_at` = placeholders**. Ces
  indicateurs dépendent de la table `posts` (Epic 3). La vue les expose à 0 / `updated_at`
  ; l'UI affiche « — » quand c'est 0. À l'Epic 3, la vue sera étendue (sous-requêtes sur
  `posts`) **sans changer le contrat** côté front (`ClientOverview` inchangé). La colonne
  « À valider » est déjà en place pour éviter un remaniement d'UI.
- **Vue plutôt que fonction** : lecture simple, jointure unique, RLS via `security_invoker`.
  À l'échelle cible (≤ 50 clients) aucune vue matérialisée nécessaire.
- **Tri client-side** : la liste est petite ; le tri se fait en mémoire sur le résultat déjà
  filtré. Tri serveur si la volumétrie explose (peu probable).
- **`ClientsPage` bascule de `listClients` → `listClientOverview`** ; `useClients` reste
  pour la compat mais la page liste utilise `useClientOverview`.

### Emplacements

- `supabase/migrations/0007_client_overview.sql`
- `src/services/clients.ts` (`listClientOverview`)
- `src/app/clients/ClientsPage.tsx` (réécrite)

### Testing

- **Intégration** : `tests/integration/client-overview.integration.test.ts` — vérifie
  l'isolation de la vue + l'exactitude de l'avancement d'onboarding.
- **Composant** : `ClientsPage` avec `listClientOverview` + `auth` mockés (dont un test de
  tri).

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-30 | v1.0 | Implémentation — Epic 2 terminé | Dev (BMad) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-5

### Debug Log References

- `npm run db:apply 0007` + `gen:types` — ok
- `npm run lint` (corrigé : `aria-sort` déplacé du `<button>` vers le `<th>`) / `typecheck` — OK
- `npm run test` — 87 tests verts

### Completion Notes List

- Indicateurs « posts en attente » et « dernière activité » : réels à l'Epic 3, placeholders ici.
- Contrat `ClientOverview` figé pour absorber l'extension sans toucher l'UI.

### File List

- `supabase/migrations/0007_client_overview.sql`
- `src/services/clients.ts` (modifié), `src/app/clients/useClients.ts` (modifié)
- `src/app/clients/ClientsPage.tsx` (réécrite)
- `src/shared/types/index.ts` (modifié), `database.ts` (régénéré)
- `tests/integration/client-overview.integration.test.ts`
- `src/app/clients/__tests__/ClientsPage.test.tsx` (réécrit)

## QA Results

_En attente de `/qa`._
